# Blog App — DOM Manipulation Project

A simple **Blog Application** built using **HTML**, **Tailwind CSS**, and **Vanilla JavaScript**, designed to practice **DOM manipulation**, **event handling**, and **localStorage**.



## 🚀 Features

✅ **Create Post** — Add a new blog post with a title and content.  
✅ **Edit Post** — Update an existing post using a prompt modal.  
✅ **Delete Post** — Remove a post instantly from the page and localStorage.  
✅ **Persistent Storage** — Posts are saved in **localStorage**, so they stay even after refreshing the page.  
✅ **Interactive Header** — Hover effects on the header using JavaScript event listeners.  
✅ **Responsive Design** — Styled with **Tailwind CSS** for a clean and mobile-friendly UI.  



## 🧠 Key Concepts Practiced

- DOM manipulation (`createElement`, `appendChild`, `innerHTML`, `closest`, etc.)
- Event Delegation (`click`, `mouseover`, `mouseout`, `input`)
- Data Persistence using `localStorage`
- Dynamic UI rendering
- Template literals and ES6 features



## 🏗️ Tech Stack

| Technology | Purpose |
|-------------|----------|
| **HTML5** | Structure of the web app |
| **Tailwind CSS** | Styling and layout |
| **Vanilla JavaScript (ES6)** | Core functionality and DOM logic |
| **localStorage** | Client-side data persistence |



